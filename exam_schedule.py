import json
import ical.calendar
import ical.event
import datetime
import utils


def convertExamSchduleToIcs(
    startDate: datetime.datetime,
    inputFilePath: str,
    outputFilePath: str,
    configFilePath: str,
):
    # to do
    return
